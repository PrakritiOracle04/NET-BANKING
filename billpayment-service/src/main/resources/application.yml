server:
  port: 8090

spring:
  application:
    name: billpayment-service
  datasource:
    url: ${BILLPAYMENT_DB_URL}
    username: ${BILLPAYMENT_DB_USERNAME}
    password: ${BILLPAYMENT_DB_PASSWORD}
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

security:
  jwt:
    secret: ${JWT_SECRET}

services:
  internal-api-key: ${INTERNAL_API_KEY}

springdoc:
  swagger-ui:
    path: /swagger-ui

logging:
  level:
    com.oracle.banking.billpayment: INFO
